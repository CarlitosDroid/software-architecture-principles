package com.carlitosdroid.abstraction.explicit_interfaces.shop

/**
 * A generic store that supports selling whatever thing.
 */
interface Shop {
    /**
     * Sales whatever thing define by the store.
     */
    fun sale()
}
